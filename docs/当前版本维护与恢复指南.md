# 杰哥工具 5.1 苹果风版维护与恢复指南

更新时间：2026-09-24  
当前版本：`v2026.09.24.2`（苹果风版，业务基线 `v2026.09.05.1`）

当前任务已完成，未上传 GitHub。砖纹连续材质、带原水印复制和宽高就近均分建议已完成，最新主工具交接：[2026-09-24 砖纹v2](checkpoints/20260924-brick-refinements.md)。主目录专用于杰哥工具源码，独立宁波价格表在同级 `../宁波仓价格表/`，不随本目录上传且不参与主工具运行；此前开单、图纸要求保留。客户价目表历史与迁移见[独立价格表交接](checkpoints/20260924-ningbo-customer-price-table.md)。

## 一、恢复工作时先看这里

无论是新的 Codex 任务、对话被压缩，还是换电脑继续维护，都按下面顺序开始：

1. 打开本目录 `杰哥工具5.1-苹果风`，不要修改保留的旧版 `杰哥工具5.0`。
2. 阅读根目录 `AGENTS.md`。
3. 阅读本指南链接的最新交接快照和 `docs/杰哥工具5.1-开发入口.md`；9月7日排版说明仅在追溯初版时读取，不当成当前状态。
4. 需要追溯完整业务规则时，阅读 `docs/杰哥工具4.0-需求归档.md`。
5. 恢复时先核对源码版本、用户本次目标及最近验收；有新代码改动时运行 `node --test tests/*.test.js`，不为只读状态询问重复整轮浏览器检查。
6. 代码修改后运行全部测试，并用浏览器实际操作受影响页面；纯文档变更核对路径和状态一致性即可。

把这几步交给新的维护任务，即可恢复当前工作状态，不需要重复发送此前的大量截图和聊天记录。

## 二、唯一主版本与权威数据

- 苹果风版主源码：当前 `杰哥工具5.1-苹果风` 目录。按用户最新要求，只保留源文件夹与历史交付文件夹，全部杰哥工具交付压缩包删除，今后不再生成压缩包。旧版5.0最近一版文件夹必须保留。
- 子工具共享界面：`tools/apple-ui.css`；首页外壳：`tools/apple-shell.css`。二者须随完整包交付。
- 上墙和孔心共享材质：`tools/cement-render.js`，不能漏传。各页传入真实几何，CSS不覆盖SVG内部几何。
- 产品与价格公共数据：`tools/product-data.js`。
- 宁波最新非洞石尺寸表：`docs/柔岩板价格表_最终尺寸替换版.xlsx`。
- 宁波原始重量核对资料：`docs/平方重量厚度-核对依据.docx`。非洞石规格级重量统一维护在 `tools/product-data.js` 的 `ningboConfirmedWeightProfiles`。
- 美利来原始价格表：`docs/美利来价格明细表2026.4.13.xlsx`。
- 鎏金重量、包装、DB、顺心捷达计算核心：`tools/freight-gold-core.js`。
- 宁波合装重量核心：`tools/ningbo-freight-core.js`。
- 顺心捷达费率：`tools/shunxin-rates.js`。
- 数字表单校验：`tools/input-validation.js`，开单和文字报价共用。只校验当前启用且显示的数字字段，保留隐藏旧仓草稿。
- 砖纹排版：`tools/brick-layout.html`、`brick-layout-core.js`、`brick-layout.js`、`brick-layout.css`、`mm-watermark.js`；独立几何、连续水泥颗粒及均分建议，不更改孔心/上墙的材质、物理尺寸或切割规则。
- 自动回归：`tests/` 下 `.test.js` 文件，包含业务、界面、输入校验、单墙尺寸、开料几何、共享材质、孔心两图同步、原PNG水印透明边缘、宁波专属物流首行及砖纹几何/导航/均分/原水印；价格表专项测试在外部目录，主工具当前141项。

同一业务规则必须通过公共数据或公共核心同步到所有相关页面，不能只改其中一个入口。公共脚本更新后，要同步修改所有引用页面的缓存版本参数。

## 三、当前已验收状态

2026-09-24 砖纹v2：整板材质不再重复铺贴，消除边界隐线；复制和全部导出带上墙同源PNG水印（不改字形/Alpha）。宽高各推荐严格上下最近均分值，已扣除内缝；点击采用按精确值绘图，显示6位小数并标≈，普通手工值不强制均分。100×1220/5采用97.083333…×1217.5后为12列×2行，24完整/0收边。141项回归通过，六档宽度输入不截断、图文对齐；预览、导出和模拟复制通过，另外在独立无头Chrome本地服务中从首页iframe实际点击复制，再通过原生粘贴取得2200×2620 PNG成功。未测试微信等外部聊天应用；复制仍需浏览器允许，受限时明确提示下载。证据及运行方式见最新交接。

2026-09-24 砖纹排版：固定整板1220×2440，只有砖宽/高/缝隙可调，示例60×240/5非业务固定值。用毫米净尺寸对齐起排、不等分拉伸，末端不足按实收边，板边落在槽内时显示实际余槽；限制最多6000格避免卡死。新图纸有水泥灰颗粒、凹槽、整板尺寸及小砖/缝隙局部标注，可复制PNG、下载PNG及自带材质SVG。136项回归通过；1600/1280/1024/768/390/320px无整页横溢，手机图纸区域单独横向滚动；多规格/非法参数/导出/模拟剪贴板/外壳导航验证通过。旧排序迁移只移动孔心及新增砖纹，其余原有相对顺序保留。未改价格、开单、客户价目表及其他工具图纸。

2026-09-24：独立价格表已按客户展示口径重做，后按用户要求整体移至同级 `../宁波仓价格表/`，与 GitHub 专用主工具目录分离；`../宁波仓价格表/客户版/` 只有同名网页和Excel，标题“MM苗苗柔岩板价格表”，93款产品、120组具体规格。出厂价为目录价+10元/㎡，备注“打印另加10元/㎡”；客户文件不含底价、内部说明、公式或隐藏价格。主工具数据和开单逻辑未修改；旧版双价稿仍在外部价格表根目录，仅供内部追溯。

2026-09-20：宁波完整开单增加物流首行，复用正文运费数据并区分物流、付款类型；金额保留，自提不写到付，默认物流不虚构承运商。正文运费、询价模板、其他仓及金额/KD不变。127项自动测试通过，实际浏览器验证宁波各物流、金额代付、其他五仓、重复切换、复制、人工正文与恢复，以及1440/390px显示；控制台/脚本错误为0。只改开单页内联脚本，公共脚本缓存不变。

2026-09-19：税金短标题及费用行底边对齐，多产品合计移回费用分组。开单/报价对非法数字和不能按面积计价的规格显示错误并停止自动结果/复制；浙江其它品类保留人工单片价，或人工明细加材料总额的例外。鎏金公共核心不再截断小数数量，DB同步拒绝非法数量；宁波页面不再提前取整。托盘文字共用核心统一，旧手工“拖盘”输入仍兼容转换。125项回归与受影响4页实际浏览器复测通过，五档宽度无整页横溢，console/pageerror为0。CSS、鎏金核心及新表单脚本缓存20260919-1。图纸、水印、价格与KD业务未改。

v3追加：整板/裁切板统一 #b8bbbe，双色图例改为材质图例，孔心渲染同色；整板/裁切统计与实际几何不变。119项测试全部通过；两面墙案例、SVG/PNG导出、原水印复制及下载、孔心六孔、1600/390px两档浏览器检查通过。共享材质JS缓存20260916-3，CSS仍20260916-1。

v2追加：上墙复制现在直接使用原水印的Alpha层，不再把细笔画增粗；原位置、板内裁剪和计算不变。浏览器复制/下载及与原PNG直接合成比对通过；源图保留136级非零透明度。最新测试总数见最新交接快照。

最新（2026-09-16）：上墙169%利用率错误已修正，该六块成品案例为3张；按批准水泥灰风格落实。孔心保留原示意图，下方新增实时凹孔渲染/单孔放大及复制。两页六档宽度、参数/孔位同步、无效状态和导出已检查；共115项自动测试通过。范围及限制见 `docs/上墙与孔心更新验收-20260916.md`。

此前已完成并保留：占位提示调浅、空合计无示例数、各仓紧凑布局、报备灰色重置、14项付款方式。以下为历史业务验收记录，不表示本轮重复了全部页面浏览器检查：

- 首页 9 个工具入口均能正常载入。
- 全部 10 个页面在 1440 像素桌面宽度和 390 像素手机宽度下无可见横向溢出。
- 美利来关键词检索、规格底价、红字备注、打印加价、1–4片包装费、5片起免包装、自定义费用及手工包装费均已验收。
- 美利来与宁波仓互切会清空旧产品；宁波产品会按自己的公共价格重新匹配。
- “重置下一个”会清零加工费并恢复美利来包装费自动状态。
- 快速查价可查询美利来与宁波公共价格；报备模板默认当前月份，顶部与结果区两个重置按钮共用正确逻辑。
- 宁波仓 22 项最新非洞石尺寸已经同步到公共产品数据；开单、快速查价、文字报价和宁波重量共用该数据，洞石系列保持原值。
- 初版业务回归基线80项、界面结构7项，共87项是历史记录；v1为115项，v2另增3项水印测试。5.0业务巡检案例见 `docs/整体检查报告-20260905.md`，初版界面检查见 `docs/苹果风排版更新说明-20260907.md`。

## 四、仍需人工确认的项目

`1200×600mm` 鎏金小板目前缺少经过确认的托盘长、宽、高。系统可以核算重量、包装数量和 DB，但顺心捷达体积重与最终运费必须继续提示人工询价。没有新权威数据前不得猜测或静默套用其它托盘尺寸。

海洞石、洞石拼接在历史开单公共规则与重量页存在重量口径差异。此前明确要求保留洞石资料，本轮未改其规格、价格和单品重量规则；发货前须人工核对，不应直接把两处数值视为一致。具体差异见整体检查报告。

价格表有产品但重量原表没有可确认对应关系的项目（例如花岗岩拼接、平板、马赛克）不再套泛关键词重量；开单提示人工填写。

## 五、交付与上传

每次完成修改后：

1. 更新首页版本号、上传说明、开发入口和需求归档。
2. 运行全部回归与实际浏览器检查。
3. 提供完整源文件夹，保留 `index.html`、`README-上传说明.txt`、`AGENTS.md`、`tools/`、`tests/`、`docs/`。
4. 核对文件夹内资源完整，不再创建ZIP、RAR、7z等压缩包；除非用户后续明确重新要求。
5. 保留5.0旧版最近一版及现有历史文件夹，不擅自删除。历史文档中“保留ZIP/必须打包”的旧约定已被本次用户要求取代。

## 六、长任务防丢失建议

- 已确认的业务规则必须写入需求归档，不只留在聊天中。
- 每个独立功能完成后更新本指南顶部的版本、当前事项、待办和验收链接；恢复只加载当前摘要，历史细节按需查。
- 长步骤前或准备换任务时，在 `docs/checkpoints/` 新增带时间的交接快照，不覆盖旧快照；只记录已完成/未完成/决策/边界/验证证据，不复制整段聊天和截图。
- 所有历史需求必须区分“已完成”和“当前待办”；不能看到旧截图就再次重做，样式以最新确认决定为准。
- 搜索先定位文件/函数，再读取有限片段。不要整段输出包含Base64水印的HTML，测试只返回失败详情及汇总；避免对同一结果反复检查。
- 一个大功能完成并整理好完整文件夹后，可以开启新任务继续；新任务只需提供本目录位置并要求先读本指南。
- 不要在一次消息里重复上传全部历史截图；只提供本次新增问题的截图和明确规则。
- 切换任务须用户明确要求；不会自动新建、关闭、清空聊天，也不擅改模型窗口或自动压缩配置。
- 当前5.1目录尚未被父目录Git跟踪；交接说明并非源码备份，不能依靠空的 `git diff` 判断已经保存到版本历史。禁止自动 reset/clean，提交或远端备份另按用户授权执行。
