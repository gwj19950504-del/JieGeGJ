# 2026-09-24 宁波客户价格表交接

本快照创建时主工具为 `v2026.09.20.1`，现主工具已升级至 `v2026.09.24.1`。价目表始终独立，不修改 `tools/product-data.js`、开单自动计价或其它子工具。2026-09-24 后续按用户要求将整个价格表文件夹移到主工具目录外，与 GitHub 上传目录分开；未上传、未压缩、未改旧版5.0。

## 交付

可外发文件仅在主工具同级、但不属于主工具 GitHub 目录的 `../宁波仓价格表/客户版/`：

- `MM苗苗柔岩板价格表.html`：苹果风四列价格表，可搜索、打印、下载同目录 Excel；桌面与手机均可浏览。
- `MM苗苗柔岩板价格表.xlsx`：同样四列的静态客户价，可筛选、打印；无公式、批注、隐藏成本列或外链。

旧 `../宁波仓价格表/宁波仓产品价格表.html` 与同名 Excel 为上一轮双价内部历史稿，不可外发。制作脚本、模板、专项测试及内部说明都在外部 `../宁波仓价格表/` 根目录；主工具运行时不依赖这些文件。

## 数据与规则

只读 `tools/product-data.js` 的 `ningboProductCatalog`，按产品与每一个具体规格展开，共 93 款产品、120 组规格。客户标题固定为“MM苗苗柔岩板价格表”，客户列固定为“产品名称 / 规格（mm） / 出厂价（元/㎡） / 备注”。出厂价 = 对应目录价 + 10 元/㎡；备注统一为“打印另加 10 元/㎡”。不在客户文件中放原始底价、内部推导、目录来源、特殊打印/3D例外或价格公式。

客户版价格口径与开单自动规则是不同用途；名称含“打印”或“3D”的开单特殊处理没有因此改动。未来若用户要求二者完全同步，应先确认新业务规则，再改工具。

## 验收

- 生成脚本校验目录名称、价格、规格、去重及两个文件的行数；独立只读复核逐行比对客户价均为目录价+10，120条备注一致，Excel无公式/批注/隐藏行列/底价或外链。
- 实际 Chrome 在 1440px 与 390px 验证：均显示120组、首项出厂价80元/㎡，搜索“水泥浇筑板”只显示1组，整页无横向溢出，脚本错误0。预览保存在外部 `../宁波仓价格表/预览-客户版-桌面.png` 与 `预览-客户版-手机.png`。
- 价格表验收脚本已从主工具 `tests/` 移至外部 `../宁波仓价格表/价格表验收.test.js`，单独运行1项通过；主工具回归现为135项，能够不依赖外部价格表目录独立运行。初版客户表创建时的128项为历史基线。
- Excel用 LibreOffice 转成 PDF 做了首张页面视觉复查：中文标题、产品名称、规格、价格和备注均完整显示，对齐正常；命令行首次因测试环境缺字体缓存丢失中文字形，指定系统中文字体目录后确认并非文件缺字。

重新生成：在两个同级文件夹的上一级目录运行 `python3 宁波仓价格表/生成价格表.py`（需要 Node.js 与 openpyxl），再运行 `node --test 宁波仓价格表/价格表验收.test.js`。生成脚本只读同级 `杰哥工具5.1-苹果风/tools/product-data.js`，不会修改主工具或历史稿。更新 `ningboProductCatalog` 后还需重做客户版浏览器与表格核对。
