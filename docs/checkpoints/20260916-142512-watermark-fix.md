---
status: completed
branch: main
timestamp: 2026-09-16T14:25:12+08:00
source_version: v2026.09.16.2
git_state: source-directory-untracked
---

# 复制水印字形修正交接

- 唯一维护源：`/Users/mac/Documents/杰哥工具/杰哥工具5.1-苹果风`。本次已完成，无编码待办；未上传、未打包，5.0未改。
- 用户要求复制水印使用其 `MMMM水印.png` 的字形。文件1933×2522；内嵌PNG本就与原文件逐字节一致，SHA-256为 `c4020d774ff85f9d9bf509643c22731eae013b3a7f0f823418ff9b677fb233c0`。
- 根因：`tools/wall-panel.html` 的 `transparentWatermark` 忽略原Alpha，把白色像素（含极低透明度的边缘）全部设成175，造成细笔画变粗。
- 修复：直接加载并缓存原PNG，不做像素重绘；失败时清缓存允许重试。保留原0.82叠加透明度、等比缩放、居中位置、板内裁剪。离线不依赖桌面源文件。
- 唯一功能改动是上墙复制水印；关联搜索仅该页使用这套水印，孔心复制不扩大范围，计算及共享CSS/JS不改。首页与版本断言更新为 `v2026.09.16.2`；共享样式/材质JS缓存仍为20260916-1。
- 新增 `tests/wall-watermark.test.js`：素材哈希、原图/Alpha保持与缓存、失败重试，共3项。
- 最终 `node --test tests/*.test.js`：**118/118通过**，0失败、0跳过。
- 实际浏览器：水印81263个非零Alpha像素、136级透明度、最大136，与原图保持一致；复制PNG和下载降级正常。与直接原PNG合成比对仅6个边缘像素通道相差1/255（PNG往返取整），板外改动为0，15块/60孔不变，无页面错误。剪贴板使用模拟，未覆盖用户剪贴板。
- 实际输出：`/Users/mac/Documents/杰哥工具/work/wall-final-20260916/上墙复制-原版水印修正.png`。

## 前序已完成状态

上墙开料169%问题已修复，六块案例是3张；水泥灰图纸与黑标注已应用。孔心原示意图下方已有同步材质渲染、单孔放大和复制。开单紧凑排版、占位提示、灰色重置及14种付款方式均已完成。不要重做旧截图需求。

需要追溯旧实现时才读 [v1完整交接](20260916-141400-context-handoff.md) 和 [上墙与孔心验收](../上墙与孔心更新验收-20260916.md)。维护边界和未确定业务数据见 [恢复指南](../当前版本维护与恢复指南.md)。

父目录Git分支main，5.1仍是未跟踪目录；快照不等于源码备份，不能凭空的git diff判断已提交。禁止自动reset/clean、提交或上传。下一任务先读AGENTS与恢复指南，只执行用户新要求。
