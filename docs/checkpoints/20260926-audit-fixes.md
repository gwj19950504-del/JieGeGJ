# 2026-09-26 巡检修复交接

状态：已完成；版本 `v2026.09.26.1`；未上传 GitHub。唯一维护/交付目录是本文件所在的 `杰哥工具5.1-苹果风`。不新建平行源码或压缩包，旧5.0和外部价格表不动。

用户对9月25日只读巡检的5项发现明确要求“直接修改”。只修复这5项，不重复历史已验收任务，不扩展报告中的可选打印建议。

## 已改内容

| 编号 | 修复 | 保持不变 |
| --- | --- | --- |
| QA-01 | 墙面列表改为宽×高，标签明确顺序 | 实际尺寸、方向、孔位、切割几何 |
| QA-02 | 舍入显示造成单价×片数不等于总额时，使用约单价＋小计 | 原始材料额、KD、价格、费用、手工正文 |
| QA-03 | 留缝选择框和自选输入各占完整一行 | 选项、启用条件和缝隙计算 |
| QA-04 | 宁波总重量和木箱卡片统一浅灰底、细灰边、深色数字、同圆角 | 重量和包装规则、其他业务语义色 |
| QA-05 | 多产品不同仓：停止合票运费/顺心估算，清旧结果，提示分仓询价，禁用两个运费复制 | 商品报价、主复制、手工内容；同仓和单产品恢复正常 |

单价说明示例：`6片（单片约208.38元，小计1250.26元）（70/平）`。按原始面积计价，材料费仍1250.26；手填总计2000且无额外费用时KD仍749.74。整价明细如28片×216=6048不改变格式。

不同仓拦截在 `updateQuoteFreight` 顶部，覆盖表单渲染、地址直接输入和清空地址。复制监听器继续先渲染再检查禁用，不能通过派发click绕过。条件必须同时检查多产品模式，隐藏的different选项不能阻止单产品。

## 修改入口与缓存

- `tools/order-template.html`：新增展示辅助 `piecePriceDetail`，鎏金与其他产品行复用；不改计算核心。
- `tools/quote-generator.html`：不同仓询价guard、清除状态与按钮文案。
- `tools/wall-panel.html`：列表顺序与留缝控件class。
- `tools/apple-ui.css`：仅wall-panel/ningbo-weight局部样式，仍限定screen。
- 所有10个子工具的共享CSS引用统一 `20260926-1`。砖纹独有CSS/JS、水印、运费及材质JS未改。
- `index.html`、README、AGENTS、维护指南、开发入口、业务归档已同步。

## 验证

1. `node --test tests/*.test.js`：148/148通过（原141项＋新增7项）。新增在 `tests/audit-fixes.test.js`，并更新原版本与CSS缓存断言；资源完整性覆盖全部10页。
2. `tests/audit-fixes-browser.cjs`：隔离本机Chrome，1332/916/390/320px，12组全部通过，无控制台及未处理脚本错误，无整页横向溢出。
3. 上墙列表3888×2289与图纸一致；留缝默认完整可读，自选5mm仍更新图纸；1332px下选择框宽273px，大于文本＋内距＋箭头所需约167px。
4. 宁波6张示例仍板重168KG、含箱233KG，两结果卡的背景/文字/边框/圆角一致。
5. 1332和390px下：开单实际复制的约单价明细与预览一致；材料费/KD不变，手工正文在参数变化后仍保留。
6. 1332和390px下：同仓总计1975、不同仓商品总计2275保持；不同仓旧重量/顺心金额清除，两个运费复制即使派发click也不写出内容；修改及清空地址不能恢复合票；同仓恢复293KG，单产品3张恢复179KG；非法数量继续禁用。
7. 人工复核本轮留缝、墙面列表、开单明细、宁波卡片与不同仓提示截图。独立只读复核不同仓guard及复制路径通过。

运行浏览器回归：

```sh
NODE_PATH=/Users/mac/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules PW_TEST_SCREENSHOT_NO_FONTS_READY=1 node tests/audit-fixes-browser.cjs
```

可用 `CHROME_PATH` 和 `OUTPUT_DIR` 覆盖本机路径。证据在主文件夹外 `../work/qa-fixes-20260926/`：`results.json`、各断点的 `wall-list`、`wall-gap`、`ningbo-results`、`order-detail`、`different-freight` PNG。不混入GitHub上传目录。原只读报告在 `../work/qa-20260925/巡检报告.md`，保留历史事实，不改写成当前缺陷。

## 范围与限制

- 复制文本在隔离浏览器中捕获，未覆盖微信等外部聊天软件的真实粘贴；本轮未改图片复制或水印。
- 没有验证Safari/Windows、系统打印、极端批量性能或外部运价是否最新；不宣称全工具绝无Bug。
- 中性卡片仅改屏幕样式，保留原打印规则。未改变任何运费费率、产品价格、KD或材料总额算法。
- 后续从这份交接及维护指南恢复，无需重发长历史截图。
