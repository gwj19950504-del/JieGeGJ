# 巡检修复验收 · v2026.09.19.1

日期：2026-09-19。用户授权修复巡检9项问题并提供完整文件夹；未改旧5.0，未上传GitHub或生成压缩包。

## 修复清单

| 原编号 | 修复 | 验收 |
|---|---|---|
| Q-01 | 税金短标题、费用字段一致外距和底边对齐 | 916px三个输入框顶部差小于1px，截图目视检查 |
| Q-04 | 多产品合计移入费用区，不再占备注列 | 916px费用/备注顶端对齐，合计连续显示；同仓两产品576元正常 |
| Q-02 | 数字约束执行，非法负数量不再出负报价 | -1两次阻断，点击处理器不能绕过；修正恢复 |
| Q-03 | 单/多产品未知规格不按零面积计价 | 未知规格提示并禁用复制；有效尺寸恢复，第二产品同样校验 |
| ORDER-001 | 所有当前有效数字输入共用校验 | 六主仓负合计阻断；负材料改写阻断；隐藏旧仓字段不误拦 |
| ORDER-002 | 其它品类自动面积必须有效 | 浙江/宁波/美利来覆盖；人工单价及人工明细+材料总额例外正常 |
| FREIGHT-001 | 页面/公共核心保留原始数量并严格校验整数 | 1.5两次阻断；2张恢复106KG；核心不忽略混装非法项 |
| NINGBO-001 | 宁波页面不再提前取整，非法草稿不参与合装 | 1.5两次不加入且不能复制；3张恢复149KG；已有明细时非法草稿也阻断 |
| FREIGHT-002 | 共用核心统一“托盘”，开单兼容旧手工文字 | 页面及复制结果显示2米托盘，3个核心引用同步缓存 |

## 自动测试

`node --test tests/*.test.js`：125项通过，0失败。新增6项输入校验回归，覆盖核心严格数量、页面不截断、隐藏/禁用字段、人工例外、多产品规格与布局结构。原水印、中灰、开料、价格和KD回归保留。

## 浏览器验收

脚本：`tests/validation-browser.cjs`，需本地Playwright；`CHROME_PATH`可指定Chrome路径，`OUTPUT_DIR`可指定证据输出目录。默认仅使用临时浏览器环境和模拟剪贴板。

- 文字报价、开单、鎏金运费、宁波重量分别检查1332/916/660/390/320px，无整页横向溢出。
- 实际点击生成/复制，验证非法状态、修正恢复、程序触发复制也不能绕过。
- 文字报价保留人工900元正文，正常多产品合计576元。
- 税金及多产品费用区截图已目视检查。
- console error / pageerror为0。

本轮原始证据在源目录同级 `work/qa-fixes-20260919`；完整工具文件夹自带测试与本验收记录，运行不依赖该证据目录。

## 边界

- 不改变价格、费率、取整、KD计算、图纸几何、中灰材质、水印。
- 不因修复输入校验推定退款业务；遵守当前输入非负约束，KD计算结果仍允许为负。
- 未验证真实操作系统剪贴板权限、外部物流价格和打印；1200×600鎏金托盘尺寸待人工确认、洞石重量口径差异等既有边界保留。
- 修复已确认场景不等于保证所有商品/浏览器组合无缺陷。窄屏图纸/表格现有横向滚动方案未作为本轮bug修改。
