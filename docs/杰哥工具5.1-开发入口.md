# 杰哥工具 5.1 · 苹果风版开发入口

当前版本：`v2026.09.16.3`，2026-09-16。

上下文恢复先读 [维护与恢复指南](当前版本维护与恢复指南.md) 及其最新交接快照。上墙与孔心需求已完成、当前无编码待办；不要按下方历史记录重做已验收任务。后续以用户本次新要求为准。

## 当前任务与边界

用户要求首页和所有子工具按照苹果风优化排版，同时用颜色区分重点，并单独建文件夹保留老版本。已从5.0完整复制到本目录，未修改旧版。初始更新仅调整显示层；v4按用户最新要求为上墙排版增加单墙自定义板宽、板高，图纸及汇总按每墙有效规格计算。价格、开单和其他工具业务不变。

## 维护入口

- 首页导航、品牌与版本：`index.html`、`tools/apple-shell.css`。
- 九个子工具共享视觉：`tools/apple-ui.css`，通过 `body.apple-ui[data-page]` 区分特殊布局。
- 共享CSS视觉层只在 `@media screen` 生效，保留已有打印媒体规则。2026-09-16按用户批准，图纸SVG的配色与凹孔由各图纸页面及 `tools/cement-render.js` 绘制，保持物理尺寸及孔位；CSS不覆盖SVG内部几何。
- 公共价格和核心仍为原来的4个JS。源价格表与全部历史业务文档完整保留。
- 测试：`node --test tests/*.test.js`；包含开单、费用、占位提示、墙面独立尺寸、原板几何不变量、169%案例、关联整板切割、共用凹孔、水印及中灰板面检查。最新数量和浏览器验收见恢复指南顶部的最新交接快照；v1验收保留在 `docs/上墙与孔心更新验收-20260916.md`。
- 交付：只提供包含 `index.html`、`tools/`、`docs/`、`tests/`、`README-上传说明.txt`、`AGENTS.md` 的完整文件夹，不能漏掉新CSS。用户最新要求删除交付压缩包，以后不再生成ZIP等压缩包。

## 色彩与层级约定

- 蓝色：主要操作、选中、键盘焦点。
- 绿色：金额、计算汇总、底价；最低售价保留红色并带文字标签，不能只靠颜色识别。
- 橙色：待确认、人工核对等提醒。
- 红色：删除和错误状态；人员编号保留原有六色体系。
- 系统字体、统一44px输入与操作控件、白色分组、浅灰工作区；避免多层粗边框和大面积纯黑结果区。

## 后续修改注意

2026-09-16 v3：上墙整板/裁切统一中灰 #b8bbbe，图旁双色图例改为“水泥板 · 中灰”。两页统一使用 `CementRender.palette.board`，孔心板面与放大区同步；共享JS缓存20260916-3，CSS不变。只改显示，`p.full`分类、数量、尺寸、开料、水印和孔位保持不变。119项回归及两页桌面/手机、复制/导出检查通过。

2026-09-16 v2：修正上墙复制的水印字形。内嵌PNG与用户提供的MMMM水印.png逐字节相同；旧处理按RGB亮度覆盖源Alpha，使半透明抗锯齿边缘变粗。`transparentWatermark`现在直接返回并缓存原图，失败清缓存可重试；保留现有0.82叠加透明度、等比缩放、居中及板内裁剪。不改孔心输出、图纸计算或共享CSS/JS，因此共享缓存版本仍为20260916-1。水印回归在 `tests/wall-watermark.test.js`。

2026-09-16：用户确认整页水泥灰预览并要求正式修复开料，同时在孔心排版原示意图下方新增参数同步的材质渲染图。两页复用 `cement-render.js`（凹孔上暗下亮、灰色孔底；不放大实际孔径）。上墙原板开料由层架改为互不重叠的余料矩形分割，检查实际放置方向的宽高，尝试两个切分方向并选择合法方案；返回前核验边界、重叠和成品清单。6块案例为3张，支持1590+660共用同条余料。显示利用率保留1位小数，不强行截断100%；无法切割时原板合计提示待核对。仅该页统计从绿底改为中性灰，其他工具金额语义色不变。CSS缓存20260916-1，方向按钮两行居中且尺寸随当前有效板材更新。

2026-09-12：开单付款方式在原11项后增加云苗个体户、安苗个体户、悦苗个体户，复用paymentMethod事件及结果生成。桌面paymentMethodGroup固定五列（14项三行），680px以下三列；只调整此按钮组，渠道不变。新增选项不触发支付宝特定扣点，不新增费率假设。缓存20260912-1。

2026-09-11 v6：打孔hint底部对齐网格、最小44px并flex居中，避免按含标题的整行居中而偏上。新增concrete-surface-options作用于硅胶膜/多尺寸附加费组，1101px以上选项占2/3、说明占右1/3，包装提示保留全宽；窄屏不改变原换行。缓存20260911-6，松诺及168四档视口检查通过。

2026-09-11 v5：报备resetTop/resetSide使用中性次级按钮：#f2f2f7底、#48484a文字、500字重、透明边框，无装饰阴影；悬停#e8e8ed、按下#dcdce2。替代历史橙色重置，复制蓝色及按钮尺寸不变。桌面手机外观、两处重置与焦点验证通过。CSS缓存20260911-5。

2026-09-11 v4：全开单体系分组统一12px顶部内距、0底距、0纵向外距，form-grid纵向gap12px；hint底距0。只限 order-template，不改其他工具。所有空追加产品容器（additionalProducts/otherAdditionalProducts/concreteAdditionalProducts/cementAdditionalProducts）不占位；生成内容后自动参与布局。混凝土首项及动态项桌面用六列网格：款式占四列、规格占两列，面积/价格/材料费各两列，匹配工具独占整行且内部三列。共享样式缓存20260911-4。六档屏宽覆盖六类主仓及混凝土/水泥板子仓，检查空白、五产品增减及重置。

2026-09-11 v3：水泥板 `.cement-card` 桌面采用2:1:1:1四列，产品名不再独占整行，单价 `.unit-price-field` 清除旧内边距和外框；`#cementAdditionalProducts:empty` 不参与布局，卡片底部内边距归零。980px及以下两列、680px及以下单列。首项及动态项共用规则；四仓各五项对齐、产品数增减保留输入、辅料开关与六档屏宽已验证。共享CSS缓存20260911-3。

2026-09-11 v2 最新补充：修补剂整体桌面固定四列，卡片内部max-content双列紧邻，不使用1fr在规格与数量间拉出留白；数量48px、规格14px、内边距6px、间距8px。内容视口980px以下两列、560px以下单列，禁止桌面三加一。缓存20260911-2；此条替代下方v1的56px数量及自动列数。

2026-09-11：以用户五张截图要求为准，替代此前修补剂两行卡片约定。单项采用两列（规格/数量）同行，数量56px；打孔费卡片控件底边对齐。报备新增 copyTop 复用 copyOutput，左右两套按钮均为“复制/重置”，左侧72px、右侧原两列宽度，蓝/浅橙区分。宁波重量选中产品用高优先级内描边覆盖旧4px外扩阴影；不移除键盘焦点。共享样式缓存20260911-1。已按1812/1332/916/660/390/320内容宽度验证，并检查手工报备复制、两处重置和修补剂金额。

2026-09-08 v2：按用户明确要求，所有 input/textarea 的占位提示在共享 `apple-ui.css` 中统一为 #74747d、opacity 0.5、font-weight 400，只作用于 ::placeholder。不得降低输入框整体或已填写文字透明度；不影响标签、金额语义色、打印和图纸。九页CSS缓存版本同步为20260908-2。首页无占位输入；动态表单自动继承统一规则。

2026-09-08：开单模板所有仓共用 `totalAmount`，删除原 `placeholder="1957"`，未填写应完全空白。合计仍由用户手填，不能借此修改金额、税金或KD业务，也不清除用户已填金额。检查范围包括六类仓库、混凝土松诺/168及水泥板苏州/华中/淮海/昌盛子仓。

v4：`tools/wall-panel.html` 通过 `sharedBoardSize` 保存统一尺寸，每墙 `useCustomBoardSize` 和 `customBoardSize` 保存开关及草稿。未勾选时修改统一尺寸，已自定义墙不受影响；取消后恢复共享，再勾选保留草稿。新增、重置及示例墙使用共享尺寸。切换输入只读取有效规格，不覆盖统一值。所有图纸、导出、原板开料和汇总共用 `layout(wall)`；汇总用每块板的 `full` 属性区分整板和裁切。任一有效板材尺寸无效时清空结果并禁止导出。排版方向、板缝、原板和孔位默认设置仍是全局参数。

v3按用户最新要求，以5.0侧栏为尺寸参考：108px宽、15px导航字、4px按钮间距，保留独立按钮与蓝色选中；按钮44px高，桌面品牌标题居中不另占图标列。手机仍为原横向导航。仅修改首页外壳，子工具及其CSS保持v2不变。

用户已确认 `v2026.09.07.2` 这套风格可用，后续先沿用，不再主动整体换风格；电脑端仍作为重点。用户已澄清需要保留的旧版为5.0。其最近版本 `v2026.09.05.1` 的源目录 `../杰哥工具5.0` 和历史交付目录 `../outputs/杰哥工具5.0-GitHub完整包-20260905-v1` 继续保留不覆盖；后续维护在本5.1目录进行。根据最新要求，全部杰哥工具交付ZIP删除，以后只保留和交付文件夹。其他历史文件夹不擅自删除。

旧HTML保留了历史样式层，本轮通过共享样式收敛，未冒险大规模删除旧CSS。后续优先在共享层做有范围的修正；若重构历史CSS，须单独检查动态表单、下拉列表、手机断点、打印与导出。

v2根据用户截图修正修补剂展开布局：数量输入不可继承通用100%宽度，规格勾选行不可嵌套通用选中边框。使用两行卡片、80px数量框和4/2/1列断点；同时压缩侧栏品牌区、桌面导航使用17px独立按钮。四种修补剂均要检查勾选、禁用数量、合计与收起展开状态。

原 `docs/杰哥工具5.0-开发入口.md` 和 `docs/整体检查报告-20260905.md` 是历史记录，并非此次界面更新的验收报告。业务遗留待确认项见维护与恢复指南；不得将本次界面检查解释为未知包装/重量已获确认。
