# 杰哥工具 5.1 苹果风版开发约束

2026-09-24 主工具当前版v2026.09.24.2：砖纹纹理改为整板连续图，取消重复贴图；复制/PNG/SVG均带原版M·M水印（tools/mm-watermark.js与上墙PNG字节一致）。宽高分别推荐上下最近均分尺寸，扣除n−1道内缝，支持点击采用；循环小数显示6位而几何按精确均分，普通输入保留最多6位。141项回归、六档布局及真实浏览器图片复制→粘贴通过。最新交接 `docs/checkpoints/20260924-brick-refinements.md`，历史截图不再当待办。

2026-09-24 主工具当前版v2026.09.24.1：新增砖纹排版，导航最后两项为孔心排版、砖纹排版。固定整板1220×2440mm，砖宽/砖高/横竖同宽缝隙可调；60×240/5仅为示例。砖纹独立使用参考照片的灰色颗粒及凹槽效果，不覆盖孔心/上墙既有中灰材质。136项回归及新页/导航浏览器验收通过。最新交接 `docs/checkpoints/20260924-brick-layout.md`。

2026-09-24 价格表已移出本 GitHub 专用主工具目录，现位于同级 `../宁波仓价格表/`；可外发文件仅在外部文件夹的 `客户版/`，其生成脚本和专项测试也独立存放。主工具运行页不依赖价格表文件夹，`node --test tests/*.test.js` 必须能在单独复制/上传本目录时通过。当前主工具135项回归，价格表另有1项专项测试；主工具数据和开单逻辑未修改。价格表历史交接见 `docs/checkpoints/20260924-ningbo-customer-price-table.md`。

2026-09-20 当前版v2026.09.20.1：仅宁波仓完整开单信息增加物流首行，正文运费保持；其他五仓和所有金额计算不变。127项测试及实际浏览器验收通过。最新交接为 docs/checkpoints/20260920-ningbo-freight-header.md。

2026-09-19 当前版v2026.09.19.1：巡检9项问题已修复；最新交接为 docs/checkpoints/20260919-validation-layout-fixes.md。新增input-validation.js，非法数字/未知面积不能生成可复制的自动金额；合法人工单价及人工明细+材料总额保留。后续只按新需求继续，不重复修复历史截图。

上下文恢复入口（2026-09-16）：任务开始、自动压缩后或换任务时，先读 `docs/当前版本维护与恢复指南.md` 及其最新交接快照，再按本次新需求定位源码。功能完成时更新当前状态与验收链接；历史截图不自动视为待办。输出定位片段和错误摘要，禁止整段输出 Base64 水印等大资源；不自动新建/清空任务或改变压缩配置。

2026-09-16 最新约定：用户要求所有板面统一中灰色（#b8bbbe），不再用深灰区分裁切板；整板/裁切判定与统计保留。上墙图纸保留黑色标注、凹入圆孔和深蓝复制按钮。孔心排版保留上方尺寸示意图，下方新增同参数渲染图；两页共用 `tools/cement-render.js`，只变材质，不改变物理孔位、孔径。原板开料必须按实际几何验证边界、重叠、方向和数量，不可用面积折算或截断利用率掩盖错误。

2026-09-11 最新局部界面约定：修补剂桌面四项同行、规格紧邻数量；报备复制蓝色、重置浅灰底深灰字（v5取代历史浅橙色），左上各72px、右侧保留原两列宽度。整体苹果风和108px侧栏继续保持。

1. 用户于 2026-09-07 明确要求苹果风新版单独建文件夹、旧版文件夹保留。本目录 `杰哥工具5.1-苹果风` 是苹果风版维护源目录；原 `杰哥工具5.0` 及历史交付文件夹保留不动。用户最新要求删除全部杰哥工具交付压缩包，以后也只保留文件夹。后续苹果风修改从本目录继续，不再随意创建平行副本。
2. 开始业务修改前，先阅读 `docs/杰哥工具4.0-需求归档.md`；如用户提出新规则，以最新明确要求为准，并同步更新归档文档和测试。
3. 涉及同一业务时必须检查所有相关子工具，不能只改一个入口。
4. 优先修改公共数据和核心：
   - 产品/价格：`tools/product-data.js`
   - 鎏金重量、包装、DB、顺心捷达：`tools/freight-gold-core.js`
   - 宁波合装：`tools/ningbo-freight-core.js`
5. 修改公共 JS 后同步更新所有页面的缓存版本参数。
6. 完成后运行 `node --test tests/*.test.js`，并对受影响页面做实际浏览器检查。
7. 正式交付只提供完整源文件夹，保留 `index.html`、`README-上传说明.txt`、`tools/`、`tests/`、`docs/` 和必要资源。遵循用户最新要求，不再生成、保留或交付ZIP、RAR、7z等压缩包；除非用户后续明确重新要求压缩包。本条取代历史文档中的打包及ZIP保留要求。
8. 不删除或重写用户未要求调整的业务规则；不支持的规格必须提示人工确认，不得静默回退。
9. 先阅读 `docs/杰哥工具5.1-开发入口.md`。共享界面样式维护在 `tools/apple-ui.css`，首页外壳在 `tools/apple-shell.css`；保留语义色、人员编号色、键盘焦点和手机布局。不要把界面样式扩展到 SVG 内部几何或打印媒体。
10. 用户于 2026-09-07 确认苹果风可用，后续先沿用语义色与修补剂两行布局；最新补充要求侧栏宽度、字体、间距参考5.0，现已在 `v2026.09.07.3` 调整为108px侧栏、15px导航字、4px间距（替代v2的172px/17px/8px），保留独立按钮、蓝色选中与44px点击高度。不主动整体换风格，电脑端优先验证。
11. 用户已澄清此前所指旧版为5.0。必须保留5.0最近一版以备使用：`../杰哥工具5.0`，以及历史交付文件夹 `../outputs/杰哥工具5.0-GitHub完整包-20260905-v1`（业务版本 `v2026.09.05.1`）。对应ZIP按用户最新要求删除。后续修改继续在本5.1目录进行，不覆盖或删除旧版文件夹。
